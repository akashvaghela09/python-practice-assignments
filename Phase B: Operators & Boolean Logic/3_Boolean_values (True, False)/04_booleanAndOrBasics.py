# Assignment 4: Combine booleans with 'and' / 'or'
# Goal: Compute two results and print them.

has_id = True
has_ticket = False

# TODO: allowed_in should be True only if both has_id and has_ticket are True
allowed_in = 

# TODO: needs_help should be True if either has_id is False OR has_ticket is False
needs_help = 

# Expected output (two lines):
# False
# True
print(allowed_in)
print(needs_help)
