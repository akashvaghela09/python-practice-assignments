# Goal: Implement a function that behaves like range(start, stop, step) for positive step.
# It must return a list of numbers (not a range object).
# Assume: step > 0.

def my_range(start, stop, step):
    values = []
    # TODO: add numbers starting at start, increasing by step, stopping before stop
    _____
    return values

print(my_range(2, 11, 3))
print(my_range(0, 1, 1))
print(my_range(5, 5, 2))

# Expected outcome:
# [2, 5, 8]
# [0]
# []
