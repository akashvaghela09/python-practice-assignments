# Task: Use zip to create formatted pairs.
# Expected outcome:
# pairs should be exactly: ['a:1', 'b:2', 'c:3']

letters = ["a", "b", "c"]
nums = [1, 2, 3]

pairs = []
for ltr, num in __:
    pairs.append(__)

print(pairs)
