# Task: Flatten a list of lists into a single list (one level deep).
# Constraints: Do not use sum(...), itertools, or list comprehensions.
# Expected outcome:
# flat should be exactly: [1, 2, 3, 4, 5, 6]

matrix = [[1, 2], [3], [4, 5, 6]]
flat = []

for row in __:
    flat.__

print(flat)
