# Goal: flatten a nested list (one level deep).
# Expected outcome (print output):
# flat=[1, 2, 3, 4, 5, 6]

matrix = [[1, 2], [3], [4, 5, 6]]

# TODO: flatten matrix into flat using a loop (not using external libraries)
flat = []


print(f"flat={flat}")
