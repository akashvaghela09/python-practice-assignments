# Goal: remove duplicates while preserving the original order.
# Constraints: build a new list named unique; do not use set() to produce the final answer directly.
# Expected outcome (print output):
# unique=["a", "b", "c", "d"]

items = ["a", "b", "a", "c", "b", "d", "d"]

# TODO: build unique preserving first occurrences only
unique = []


print(f"unique={unique}")
