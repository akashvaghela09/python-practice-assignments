# Given a 3x3 matrix (nested list), compute the sum of each row.
# Expected outcome (exact):
# [6, 15, 24]

matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

row_sums = []
# TODO: for each row in matrix, compute its sum WITHOUT using sum()
# TODO: append each row total to row_sums in order

print(row_sums)
