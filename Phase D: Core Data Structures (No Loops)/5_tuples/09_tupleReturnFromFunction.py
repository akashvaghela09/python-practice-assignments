# Complete the function so it returns a tuple (minimum, maximum) from the input tuple of integers.
# Then call the function with values and print the returned tuple.
# Expected output: (2, 9)

def min_max(values):
    # TODO: return (min_value, max_value)
    return 

values = (5, 2, 9, 3)
result = min_max(values)
print(result)
