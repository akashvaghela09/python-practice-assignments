# Goal: Flatten a nested structure up to two levels deep
# Expected outcome: running this file prints exactly: [1, 2, 3, 4, 5, 6]

nested = [[1, [2, 3]], [4, [5, 6]]]

# TODO: build flat so all ints appear in left-to-right order
# Hint: you may use loops and type checks; no recursion needed for exactly this shape.
flat = None

print(flat)
