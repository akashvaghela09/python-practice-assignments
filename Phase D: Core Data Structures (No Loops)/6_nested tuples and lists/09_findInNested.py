# Goal: Find coordinates of a target in a 2D list
# Expected outcome: running this file prints exactly: (2, 0)

grid = [
    [".", ".", "."],
    [".", "X", "."],
    ["T", ".", "."]
]

target = "T"

# TODO: set pos to a tuple (row_index, col_index) where target is found
pos = None

print(pos)
