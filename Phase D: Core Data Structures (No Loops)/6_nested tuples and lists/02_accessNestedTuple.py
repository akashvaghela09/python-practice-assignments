# Goal: Access a value inside a nested tuple
# Expected outcome: running this file prints exactly: alice

users = ("root", ("bob", "carol"), ("dave", "alice"))

# TODO: set name to "alice" by indexing into users
name = None

print(name)
