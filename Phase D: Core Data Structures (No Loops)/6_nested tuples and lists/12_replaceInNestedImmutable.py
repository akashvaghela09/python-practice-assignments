# Goal: Replace a value inside a nested tuple by creating a new tuple
# Expected outcome: running this file prints exactly: ('a', ('b', 'Z'), 'c')

data = ("a", ("b", "x"), "c")

# TODO: create new_data where the inner "x" becomes "Z"
new_data = None

print(new_data)
