# Ask the user for three comma-separated colors (example: red,blue,green).
# Create a list named colors containing the three values with whitespace removed.
# Print colors.
# Expected outcome example for input "red, blue, green": ['red', 'blue', 'green']

raw = input("Enter three colors separated by commas: ")
colors = 
print(colors)
