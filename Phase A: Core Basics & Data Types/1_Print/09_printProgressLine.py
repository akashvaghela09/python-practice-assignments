# Goal: Print progress updates on the SAME line, ending with a newline at the end.
# Expected output (what the user sees after the program finishes):
# Loading... 100%

# TODO: Use carriage return (\r) and end='' to overwrite the line.
# TODO: Ensure the final state ends with a newline.
print()
print()
print()