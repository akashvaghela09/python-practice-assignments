# Goal: Use variables to keep a running total.
# Program reads 3 integers (each on its own line).
# Expected output (exactly 1 line, for inputs 4, 1, 9):
# total=14

n1 = int(input())
n2 = int(input())
n3 = int(input())

# TODO: compute total using a running total variable

print("total=" + str(total))
